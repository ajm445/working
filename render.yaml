services:
  - type: static_site
    name: japan-working-holiday-app
    buildCommand: npm install && npm run build
    publishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
